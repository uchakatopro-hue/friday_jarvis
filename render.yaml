services:
  - type: web
    name: friday-jarvis
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python agent.py dev
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: PYTHON_VERSION
        value: "3.11.6"
    plan: free
